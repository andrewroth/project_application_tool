require File.expand_path(File.dirname(__FILE__) + '/../../spec_helper')

describe "/eventgroup_coordinators/list.html.erb" do
  include EventgroupCoordinatorsHelper
  
  before(:each) do
    assigns[:eventgroup_coordinators] = [
      stub_model(EventgroupCoordinator,
        :viewer_id => 1
      ),
      stub_model(EventgroupCoordinator,
        :viewer_id => 1
      )
    ]
    assigns[:eg] = stub_model(EventGroup, :id => 1)
  end

  it "should render list of eventgroup_coordinators" do
    render "/eventgroup_coordinators/list.html.erb"
    response.should have_tag("tr>td")
  end
end


<% @flags.each do |flag| %>	
	<%= check_box('page', :"is_#{flag.name}?") %> <%= flag.group_txt.sub('group', 'page') %>
    <%= observe_field("page_is_#{flag.name}?",
									:url => {:action => "set_flag", :id => @page.id, :flag => flag.name},
									:with => 'value')%>
	<BR>
<% end %>
<BR>

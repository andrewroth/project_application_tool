<%- @readonly = true %>
<% for field in [ 'Name', ['Rship','Relationship'], 'Home', 'Work', 'Mobile', 'Email' ] %>
  <% if field.class == Array then suffix, label = field else suffix = label = field end %>
  <%= q label, @emerg.send("#{prefix}#{suffix}") %>
<% end %>

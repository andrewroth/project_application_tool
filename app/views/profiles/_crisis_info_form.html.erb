<P>
What do you normally go by?

First name: <%= text_field :person, :person_fname %>
Last name: <%= text_field :person, :person_fname %>

<P>
What is your formal name on your passport?

Legal first name: <%= text_field :person, :person_legal_fname %>
Legal last name: <%= text_field :person, :person_legal_fname %>

<P> Gender: <%= collection_select :person, :gender_id, Gender.find(:all), :id, :gender_desc %>

<P> Email: <%= text_field :person, :person_email %>

<P> Current Province: <%= collection_select :person, :person_local_province_id, Province.find(:all), :id, :province_desc %>

<P> Permanent Province: <%= collection_select :person, :province_id, Province.find(:all), :id, :province_desc %>

<P> Birthdate: <%= date_select :emerg, :emerg_birthdate, :start_year => 1950, :end_year => 10.years.ago.year %>

<P> Passport #: <%= text_field :emerg, :emerg_passportNum %>

<P> Passport Exp: <%= date_select :emerg, :emerg_passportExpiry %>

<P> Passport Nationality: <%= text_field :emerg, :emerg_passportOrigin %>

<P> Do you have any medical conditions that could potentially become an emergency?  If so, please explain.: <BR /><%= text_area :emerg, :emerg_medicalNotes, :rows => 5, :cols => 80 %>

<P> Medication(s) Needed:  Will you be traveling with this medication on project? <BR /><%= text_area :emerg, :emerg_meds, :rows => 5, :cols => 80 %>

<HR>

<P> Emergency Contact 1: <%= render :partial => 'profiles/crisis_info_emerg_contact', :locals => { :prefix => 'emerg_contact' } %>

<HR>

<P> Emergency Contact 2: <%= render :partial => 'profiles/crisis_info_emerg_contact', :locals => { :prefix => 'emerg_contact2' } %>

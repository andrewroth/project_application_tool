<%- default_first ||= false -%>
<%- include_all ||= false; include_all = false if @project_with_full_view.length == 1 -%>
<%= select_tag "#{action}_project_id", options_for_select((default_first ? [] : [[""]]) + @project_with_full_view.collect{ |p| [ p.name, p.id ] } + (include_all ? [[ "All", "all"]] : [])), :name => "project_id" %>

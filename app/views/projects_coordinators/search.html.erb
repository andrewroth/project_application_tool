<ul>
<% for person in @people %>
  <% next unless person.viewer %>
  <li><%= link_to "#{person.name} (#{person.viewer.profiles.length} profiles)" , { :action => :create, 'projects_coordinator[viewer_id]' => person.viewer.id }, { :method => :post } %>
<% end %>
</ul>
